<%= form_for(@sales_tax) do |f| %>
  <% if @sales_tax.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sales_tax.errors.count, "error") %> prohibited this sales_tax from being saved:</h2>

      <ul>
      <% @sales_tax.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sales_tax %><br>
    <%= f.text_field :sales_tax %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
